export const STATE = Object.freeze({
    RAW: `raw`, 
    COOKED: `cooked`,
    PREPPED: `prepped`
});

export const ACTIONS = Object.freeze({
    NOTHING: `nothing`,
    PREP: `preparation`,
    COOK: `cook`
});

export const EMPTY_STATE = Object.freeze({
    IMAGE: `src/widget/images/placeholder.jpg`
});
